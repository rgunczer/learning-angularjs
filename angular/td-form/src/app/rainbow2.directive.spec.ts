import { Rainbow2Directive } from './rainbow2.directive';

describe('Rainbow2Directive', () => {
  it('should create an instance', () => {
    const directive = new Rainbow2Directive();
    expect(directive).toBeTruthy();
  });
});
