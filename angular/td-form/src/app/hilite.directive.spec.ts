import { HiliteDirective } from './hilite.directive';

describe('HiliteDirective', () => {
  it('should create an instance', () => {
    const directive = new HiliteDirective();
    expect(directive).toBeTruthy();
  });
});
