extends layout

block content

    if games.length
        table.listing-table.table-dark.table-stripped
            tr
                th Name
                th Year
            each game in games
                tr
                    td= game.name
                    td= game.year
    else
        p No games